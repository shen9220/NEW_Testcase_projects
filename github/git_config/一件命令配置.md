# 1. 启动 ssh-agent 并加载密钥
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519

# 2. 测试 SSH 连接（出现 "successfully authenticated" 即成功）
ssh -T git@github.com

# 3. 配置 Git 全局用户信息
git config --global user.name "shen9220"
git config --global user.email "1436114007@qq.com"

# 4. 进入你的项目目录（此处替换为实际路径）
cd /Users/admin/New_Testcase_Projects

# 5. 如果尚未初始化 Git，则初始化
git init

# 6. 添加 SSH 远程地址
#    如果已经存在 origin，先删除，否则直接添加
git remote remove origin 2>/dev/null
git remote add origin git@github.com:shen9220/New_Testcase_Projects.git

# 7. 验证远程配置
git remote -v