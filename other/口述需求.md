我计划用 ai 工具写一个生成测试用例的工具，根据我上传的 prd 文件，ai 通过识别上传的prd 文件（一般是 md 格式，也可能会有 word 或者 pdf）来生成测试用例

需要有前端可视化页面，

你根据我提的要求帮我完善一下以上的 config 里面的准备文件，要支撑完整的需求以及各类需要给 ai 支持的文档：目录要规范化、bot 配置必须清晰、skills 必须清晰，前后端代码必须可维护，上传的 md 需求文档要求全部获取并支持阅读，最好给一个真正可以操作的测试用例和错误用例的示范等；

注意：后端生成测试用例的逻辑要贴合实际，必须要有可操作执行的测试用例，并非固化/臆想的固定测试用例

另外：我不部署在服务器上，直接本地直接启动前后端服务，支持在网页中打开这个地址并支持操作；
前端页面要有一些功能如下：输入需求模块：prd 文件上传、prd 手动输入框、prd 提交按钮（生成测试用例按钮）；生成的测试用例模块：支持 Excel 格式和 xmind 格式，支持下载，分页，删除/批量删除；日志模块：记录每次操作的日志记录；
以上所有数据全部存储在当前项目文件中，用到的是免费的个人数据库Supabase（这里要建表你再告诉我，我已经配置了部分表，具体见.env这个文件，每次生成新的文件都记录在目录中并持续更新，不覆盖之前文档）。

最后：如有其他遗漏帮我补充完整，以上所有文档内容全部帮我生成可下载的 md 文件，是所有关于这个项目的内容都要支持md 格式下载到我本地

